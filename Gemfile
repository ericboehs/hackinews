# frozen_string_literal: true

source 'https://rubygems.org'

ruby '~> 4.0'

gem 'activerecord'
gem 'dotenv'
gem 'pg'
gem 'puma'
gem 'rackup'
gem 'rake'
gem 'sinatra'

group :development do
  gem 'rerun'
end

group :development, :test do
  gem 'pry'
end

group :test do
  gem 'minitest'
  gem 'rack-test'
end
